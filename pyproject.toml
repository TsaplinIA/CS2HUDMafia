[project]
name = "cs2hudmafia"
version = "0.1.0"
description = "HUD for cs2"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0.36",
]

[tool.ruff]
exclude = ["storage/*", "assets/*"]
fix = true
line-length = 120
